name: Deploy to Firebase Hosting

on:
  push:
    branches: [ main, master ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - run: npx firebase-tools deploy --only hosting --project tanks-game-e5329 --token "${{ secrets.FIREBASE_TOKEN }}" --non-interactive
